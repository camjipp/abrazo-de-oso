import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // site: 'https://camjipp.github.io', // Comment out for Vercel
  // base: '/abrazo-de-oso', // Comment out for Vercel - use root path
  compressHTML: true,
});

